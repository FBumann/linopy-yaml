"""Engines: implementations of `relational.engine.Engine`.

One ships (`polars`). The package exists so that the boundary is a directory
rather than a convention — everything in here is swappable, everything above it
is the contract every engine answers to.
"""
